{
    "routes": [
      {
        "src": "/api/accounts",
        "dest": "./routes/account.js"
      }
    ],
    "build": {
      "commands": [
        "npm install",
        "npm run build"
      ],
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
  